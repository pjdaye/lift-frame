# Oracle-first

**Oracle-first** is the operational principle that makes bounded correctness testable at scale:

- Deterministic checks outrank subjective judgments
- Use LLM judges after invariants pass
- Meta-validate judges so dashboards don’t lie
